---
title: "単語区切り"
description: "要素の単語区切りを制御するためのユーティリティ。"
---

import plugin from 'tailwindcss/lib/plugins/wordBreak'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'
import { ResponsiveCodeSample } from '@/components/ResponsiveCodeSample'

export const classes = { plugin }

## Normal

`.break-normal`を使用して、通常の単語ブレークポイントにのみ改行を追加します。

```html
<template preview>
  <p class="break-normal max-w-xs p-2 bg-gray-200 mx-auto">
  Lorem ipsum dolor sit amet, consectetur adipisicing elit. Blanditiisitaquequodpraesentiumexplicaboincidunt? Dolores beatae nam at sed dolorum ratione dolorem nisi velit cum.
  </p>
</template>

<p class="break-normal ...">...</p>
```

## Break Words

必要に応じて、`.break-words`を使用して、単語の途中で改行を追加します。

```html
<template preview>
  <p class="break-words max-w-xs p-2 bg-gray-200 mx-auto">
  Lorem ipsum dolor sit amet, consectetur adipisicing elit. Blanditiisitaquequodpraesentiumexplicaboincidunt? Dolores beatae nam at sed dolorum ratione dolorem nisi velit cum.
  </p>
</template>

<p class="break-words ...">...</p>
```

## Break All

`.break-all`を使用して、単語全体を保持しようとせずに、必要に応じて改行を追加します。

```html
<template preview>
  <p class="break-all max-w-xs p-2 bg-gray-200 mx-auto">
  Lorem ipsum dolor sit amet, consectetur adipisicing elit. Blanditiisitaquequodpraesentiumexplicaboincidunt? Dolores beatae nam at sed dolorum ratione dolorem nisi velit cum.
  </p>
</template>

<p class="break-all ...">...</p>
```

## Truncate

必要に応じて、`.truncate`を使用して、オーバーフローしたテキストを省略記号(<code>…</code>)で切り捨てます。

```html
<template preview>
  <p class="truncate max-w-xs p-2 bg-gray-200 mx-auto">
  Lorem ipsum dolor sit amet, consectetur adipisicing elit. Blanditiisitaquequodpraesentiumexplicaboincidunt? Dolores beatae nam at sed dolorum ratione dolorem nisi velit cum.
  </p>
</template>

<p class="truncate ...">...</p>
```

## Responsive

特定のブレークポイントでのみ要素のワードブレイクを制御するには、既存のワードブレイクユーティリティに`{screen}:`プレフィックスを追加します。
たとえば、クラス`md:break-all`を要素に追加すると、中程度の画面サイズ以上で`break-all`ユーティリティが適用されます。

Tailwindのレスポンシブデザイン機能の詳細については、[レスポンシブデザイン](/docs/responsive-design)のドキュメントをご覧ください。

<ResponsiveCodeSample
  classNames={['break-normal', 'break-words', 'break-all', 'truncate', 'break-normal']}
  snippet={(classNames) => `
    <p class="${classNames} ...">
      ...
    </p>
  `}
  preview={(className) => (
    <p className={`max-w-xs p-2 bg-gray-200 mx-auto ${className}`}>
      Lorem ipsum dolor sit amet, consectetur adipisicing elit. Blanditiisitaquequodpraesentiumexplicaboincidunt? Dolores beatae nam at sed dolorum ratione dolorem nisi velit cum.
    </p>
  )}
/>

## Customizing

### レスポンシブと疑似バリアントクラス

<Variants plugin="wordBreak" />

### 無効にする

<Disabling plugin="wordBreak" />
