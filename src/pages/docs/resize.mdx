---
title: "リサイズ"
description: "要素のサイズ変更方法を制御するためのユーティリティ。"
---

import plugin from 'tailwindcss/lib/plugins/resize'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = { plugin }

## Resize in all directions

`.resize`を使用して、要素を水平方向および垂直方向にサイズ変更可能にします。

```html
<template preview>
  <textarea class="resize border rounded focus:outline-none focus:shadow-outline"></textarea>
</template>
```

## Resize vertically

`.resize-y`を使用して、要素を垂直方向にサイズ変更可能にします。

```html
<template preview>
  <textarea class="resize-y border rounded focus:outline-none focus:shadow-outline"></textarea>
</template>
```

## Resize horizontally

`.resize-x`を使用して、要素を水平方向にサイズ変更可能にします。

```html
<template preview>
  <textarea class="resize-x border rounded focus:outline-none focus:shadow-outline"></textarea>
</template>
```

## Prevent resizing

要素のサイズを変更できないようにするには、`.resize-none`を使用します。

```html
<template preview>
  <textarea class="resize-none border rounded focus:outline-none focus:shadow-outline"></textarea>
</template>
```

## カスタマイズ

### レスポンシブと疑似バリアントクラス

<Variants plugin="resize" name="resizing" />

### 無効にする

<Disabling plugin="resize" name="resizing" />
