---
title: "バックグラウンド不透明度"
description: "要素の背景色の不透明度を制御するためのユーティリティ"
featureVersion: "v1.4.0+"
---

import plugin from 'tailwindcss/lib/plugins/backgroundOpacity'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'

export const classes = { plugin }

## 使用方法

`.bg-opacity-{amount}`ユーティリティを使用して、要素の背景色の不透明度を制御します。

```html
<template preview class="bg-checkered p-4">
  <div class="flex justify-around">
    <div class="h-16 w-16 rounded bg-blue-500 bg-opacity-100"></div>
    <div class="h-16 w-16 rounded bg-blue-500 bg-opacity-75"></div>
    <div class="h-16 w-16 rounded bg-blue-500 bg-opacity-50"></div>
    <div class="h-16 w-16 rounded bg-blue-500 bg-opacity-25"></div>
    <div class="h-16 w-16 rounded bg-blue-500 bg-opacity-0"></div>
  </div>
</template>

<div class="bg-blue-500 bg-opacity-100"></div>
<div class="bg-blue-500 bg-opacity-75"></div>
<div class="bg-blue-500 bg-opacity-50"></div>
<div class="bg-blue-500 bg-opacity-25"></div>
<div class="bg-blue-500 bg-opacity-0"></div>
```

## レスポンシブ

特定のブレークポイントで要素の背景色の不透明度を制御するには、既存の背景色の不透明度ユーティリティに`{screen}:`プレフィックスを追加します。
たとえば、`md:bg-opacity-50`を使用して、中程度の画面サイズ以上でのみ`bg-opacity-50`ユーティリティを適用します。

```html
<div class="bg-blue-500 bg-opacity-75 md:bg-opacity-50">
  <!-- ... -->
</div>
```

Tailwindのレスポンシブデザイン機能の詳細については、[Responsive Design](/docs/responsive-design)のドキュメントをご覧ください。

## カスタマイズ

すべての不透明度関連ユーティリティの不透明度値を一度にカスタマイズするには、`tailwind.config.js`テーマ設定の`opacity`セクションを使用します。

```diff
  // tailwind.config.js
  module.exports = {
    theme: {
      extend: {
        opacity: {
+         '10': '0.1',
+         '20': '0.2',
+         '95': '0.95',
        }
      }
    }
  }
```

背景の不透明度ユーティリティのみをカスタマイズする場合は、`backgroundOpacity`セクションを使用します。

```diff
  // tailwind.config.js
  module.exports = {
    theme: {
      extend: {
        backgroundOpacity: {
+         '10': '0.1',
+         '20': '0.2',
+         '95': '0.95',
        }
      }
    }
  }
```

デフォルトのテーマのカスタマイズについて詳しくは、 [theme customization documentation](/docs/theme#customizing-the-default-theme)をご覧ください。

### レスポンシブと疑似バリアントクラス

<Variants plugin="backgroundOpacity" />

### 機能を使用しない場合

<Disabling plugin="backgroundOpacity" />
