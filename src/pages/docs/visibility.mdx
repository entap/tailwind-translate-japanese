---
title: "可視性"
description: "要素の可視性を制御するためのユーティリティ。"
features:
  responsive: true
  customizable: false
  hover: false
  focus: false
---

import plugin from 'tailwindcss/lib/plugins/visibility'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'
import { ResponsiveCodeSample } from '@/components/ResponsiveCodeSample'

export const classes = { plugin }

## Visible

`.visible`を使用して、要素を表示します。これは主に、さまざまな画面サイズで`.invisible`ユーティリティを元に戻す場合に役立ちます。

```html
<template preview class="flex justify-center p-4">
  <div class="bg-gray-400 w-32 h-12"></div>
  <div class="visible bg-gray-600 w-32 h-12"></div>
  <div class="bg-gray-400 w-32 h-12"></div>
</template>

<div class="flex justify-center">
  <div class="bg-gray-400"></div>
  <div class="bg-gray-600 visible"></div>
  <div class="bg-gray-400"></div>
</div>
```

## Invisible

`.invisible`を使用して要素を非表示にしますが、それでもDOM内での位置を維持し、他の要素のレイアウトに影響を与えます。（[display](/docs/display#hidden)ドキュメントの`.hidden`と比較してください）

```html
<template preview class="flex justify-center p-4">
  <div class="bg-gray-400 w-32 h-12"></div>
  <div class="invisible bg-gray-600 w-32 h-12"></div>
  <div class="bg-gray-400 w-32 h-12"></div>
</template>

<div class="flex justify-center">
  <div class="bg-gray-400"></div>
  <div class="bg-gray-600 invisible"></div>
  <div class="bg-gray-400"></div>
</div>
```

## Responsive

特定のブレークポイントでのみ可視性ユーティリティを適用するには、既存のクラス名に`{screen}:`プレフィックスを追加します。
たとえば、クラス`md:invisible`を要素に追加すると、中程度の画面サイズ以上で`invisible`ユーティリティが適用されます。

Tailwindのレスポンシブデザイン機能の詳細については、[レスポンシブデザイン](/docs/responsive-design)のドキュメントをご覧ください。

<ResponsiveCodeSample
  classNames={['visible', 'invisible', 'visible', 'invisible', 'visible']}
  snippet={(classNames) => `
    <div class="flex justify-center">
      <div class="bg-gray-400"></div>
      <div class="bg-gray-600 ${classNames}"></div>
      <div class="bg-gray-400"></div>
    </div>
  `}
  preview={(className) => (
    <div className="flex justify-center">
      <div className="bg-gray-400 w-32 h-12" />
      <div className={`bg-gray-600 w-32 h-12 ${className}`} />
      <div className="bg-gray-400 w-32 h-12" />
    </div>
  )}
/>

## Customizing

### レスポンシブと疑似バリアントクラス

<Variants plugin="visibility" />

### 無効にする

<Disabling plugin="visibility" />
