---
title: "ディスプレイ"
description: "要素の表示ボックスタイプを制御するためのユーティリティ。"
---

import plugin from 'tailwindcss/lib/plugins/display'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'
import { ResponsiveCodeSample } from '@/components/ResponsiveCodeSample'

export const classes = { plugin }

## Block

`.block`を使用して、ブロックレベルの要素を作成します。

```html
<template preview>
  <div class="bg-gray-200 p-4">
    <span class="block text-gray-700 text-center bg-gray-400 px-4 py-2">1</span>
    <span class="block text-gray-700 text-center bg-gray-400 px-4 py-2 mt-2">2</span>
    <span class="block text-gray-700 text-center bg-gray-400 px-4 py-2 mt-2">3</span>
  </div>
</template>
```

## Flow-Root

`.flow-root`を使用して、独自の[block formatting context](https://developer.mozilla.org/en-US/docs/Web/Guide/CSS/Block_formatting_context)を持つブロックレベルの要素を作成します。

```html
<template preview>
  <div class="flow-root bg-gray-400">
    <div class="my-4 block text-gray-700 text-center bg-gray-500 px-4 py-2">1</div>
  </div>
  <div class="flow-root bg-gray-200">
    <div class="my-4 block text-gray-700 text-center bg-gray-400 px-4 py-2">2</div>
  </div>
</template>
```

## Inline Block

`.inline-block`を使用して、インラインブロックレベル要素を作成します。

```html
<template preview>
  <div class="bg-gray-200">
    <div class="inline-block text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">1</div>
    <div class="inline-block text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">2</div>
    <div class="inline-block text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">3</div>
  </div>
</template>
```

## Inline

`.inline`を使用してインライン要素を作成します。

```html
<template preview>
  <div class="bg-gray-200">
    <div class="inline text-gray-700 text-center bg-gray-400 px-4 py-2">1</div>
    <div class="inline text-gray-700 text-center bg-gray-400 px-4 py-2">2</div>
    <div class="inline text-gray-700 text-center bg-gray-400 px-4 py-2">3</div>
  </div>
</template>
```

## Flex

`.flex`を使用して、ブロックレベルのフレックスコンテナを作成します。

```html
<template preview>
  <div class="flex bg-gray-200">
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">1</div>
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">2</div>
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">3</div>
  </div>
</template>
```

## Inline flex

`.inline-flex`を使用して、インラインフレックスコンテナを作成します。

```html
<template preview>
  <div class="inline-flex bg-gray-200">
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">1</div>
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">2</div>
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">3</div>
  </div>
</template>
```

## Grid

`.grid`を使用してグリッドコンテナを作成します。

```html
<template preview class="bg-white p-8">
  <div class="grid gap-4 grid-cols-3">
    <div class="bg-gray-300 h-12"></div>
    <div class="bg-gray-500 h-12"></div>
    <div class="bg-gray-300 h-12"></div>
    <div class="bg-gray-500 h-12"></div>
    <div class="bg-gray-300 h-12"></div>
    <div class="bg-gray-500 h-12"></div>
    <div class="bg-gray-300 h-12"></div>
    <div class="bg-gray-500 h-12"></div>
    <div class="bg-gray-300 h-12"></div>
  </div>
</template>

<div class="grid gap-4 grid-cols-3">
  <!-- ... -->
</div>
```

## Inline Grid

`.inline-grid`を使用して、インライングリッドコンテナを作成します。

```html
<template preview class="bg-white p-8">
  <span class="inline-grid grid-cols-3 gap-x-4 p-2 bg-gray-200">
    <span class="px-4 py-2 text-center bg-gray-400">1</span>
    <span class="px-4 py-2 text-center bg-gray-400">1</span>
    <span class="px-4 py-2 text-center bg-gray-400">1</span>
  </span>
  <span class="inline-grid grid-cols-3 gap-x-4 p-2 bg-gray-200">
    <span class="px-4 py-2 text-center bg-gray-400">2</span>
    <span class="px-4 py-2 text-center bg-gray-400">2</span>
    <span class="px-4 py-2 text-center bg-gray-400">2</span>
  </span>
</template>

<span class="inline-grid grid-cols-3 gap-x-4">
  <span>1</span>
  <span>1</span>
  <span>1</span>
</span>
<span class="inline-grid grid-cols-3 gap-x-4">
  <span>2</span>
  <span>2</span>
  <span>2</span>
</span>
```

## Contents

`.contents`を使用して、子が親の直接の子のように動作する「ファントム」コンテナを作成します。

```html
<template preview>
  <div class="flex bg-gray-200">
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">1</div>
    <div class="contents">
      <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">2</div>
      <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">3</div>
    </div>
    <div class="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">4</div>
  </div>
</template>
```

## Table

`.table`,`.table-row`,`.table-cell`,`.table-caption`,`.table-column`,`.table-column-group`,`.table-header-group`,`table-row-group`および`.table-footer-group`ユーティリティを使用して、
それぞれのテーブル要素のように動作する要素を作成します。

```html
<template preview>
  <div class="table w-full">
    <div class="table-row-group">
      <div class="table-row">
        <div class="table-cell bg-gray-400 text-gray-700 px-4 py-2 text-sm">A cell with more content</div>
        <div class="table-cell bg-gray-200 text-gray-700 px-4 py-2 text-sm">Cell 2</div>
        <div class="table-cell bg-gray-400 text-gray-700 px-4 py-2 text-sm">Cell 3</div>
      </div>
      <div class="table-row">
        <div class="table-cell bg-gray-200 text-gray-700 px-4 py-2 text-sm">Cell 4</div>
        <div class="table-cell bg-gray-400 text-gray-700 px-4 py-2 text-sm">A cell with more content</div>
        <div class="table-cell bg-gray-200 text-gray-700 px-4 py-2 text-sm">Cell 6</div>
      </div>
    </div>
  </div>
</template>
```

## Hidden

`.hidden`を使用して要素を`display:none`に設定し、ページレイアウトから削除します（[visibility](/docs/visibility＃invisible)ドキュメントの`.invisible`と比較してください）。

```html
<template preview>
  <div class="flex bg-gray-200">
    <div class="hidden text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">1</div>
    <div class="text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">2</div>
    <div class="text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">3</div>
  </div>
</template>
```

## Responsive

特定のブレークポイントで要素の表示プロパティを制御するには、既存の表示ユーティリティクラスに`{screen}:`プレフィックスを追加します。
たとえば、`md:inline-flex`を使用して、中程度の画面サイズ以上でのみ`inline-flex`ユーティリティを適用します。

Tailwindのレスポンシブデザイン機能の詳細については、[Responsive Design](/docs/responsive-design)のドキュメントをご覧ください。

<ResponsiveCodeSample
  classNames={['flex', 'inline-flex', 'block', 'hidden', 'flex']}
  snippet={(classNames) => `
    <div class="${classNames} ...">
      <!-- ... -->
    </div>
  `}
  preview={(className) => (
    <div className={`bg-gray-200 ${className}`}>
      <div className="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">1</div>
      <div className="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">2</div>
      <div className="flex-1 text-gray-700 text-center bg-gray-400 px-4 py-2 m-2">3</div>
    </div>
  )}
/>

## Customizing

### レスポンシブと疑似バリアントクラス

<Variants plugin="display" />

### 無効にする

<Disabling plugin="display" />
