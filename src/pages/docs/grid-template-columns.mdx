---
title: "グリッドテンプレートカラム"
description: "グリッドレイアウトで列を指定するためのユーティリティ。"
featureVersion: "v1.2.0+"
---

import plugin from 'tailwindcss/lib/plugins/gridTemplateColumns'
import { Variants } from '@/components/Variants'
import { Disabling } from '@/components/Disabling'
import { ResponsiveCodeSample } from '@/components/ResponsiveCodeSample'

export const classes = { plugin }

## 使用方法

`grid-cols-{n}`ユーティリティを使用して、_n_個の同じサイズの列を持つグリッドを作成します。

```html
<template preview class="bg-white p-8">
  <div class="grid grid-cols-3 gap-4">
    <div class="bg-gray-300 h-12 flex items-center justify-center">1</div>
    <div class="bg-gray-500 h-12 flex items-center justify-center">2</div>
    <div class="bg-gray-300 h-12 flex items-center justify-center">3</div>
    <div class="bg-gray-500 h-12 flex items-center justify-center">4</div>
    <div class="bg-gray-300 h-12 flex items-center justify-center">5</div>
    <div class="bg-gray-500 h-12 flex items-center justify-center">6</div>
    <div class="bg-gray-300 h-12 flex items-center justify-center">7</div>
    <div class="bg-gray-500 h-12 flex items-center justify-center">8</div>
    <div class="bg-gray-300 h-12 flex items-center justify-center">9</div>
  </div>
</template>

<div class="grid grid-cols-3 gap-4">
  <div>1</div>
  <!-- ... -->
  <div>9</div>
</div>
```

## レスポンシブ

特定のブレークポイントでグリッドの列を制御するには、既存のgrid-template-columnsユーティリティに`{screen}:`プレフィックスを追加します。
たとえば、`md:grid-cols-6`を使用して、中程度の画面サイズ以上でのみ`grid-cols-6`ユーティリティを適用します。

Tailwindのレスポンシブデザイン機能の詳細については、[Responsive Design](/docs/responsive-design)のドキュメントをご覧ください。

<ResponsiveCodeSample
  classNames={['grid-cols-1', 'grid-cols-2', 'grid-cols-3', 'grid-cols-4', 'grid-cols-6']}
  snippet={(classNames) => `
    <div class="grid ${classNames}">
      <!-- ... -->
    </div>
  `}
  previewClassName="p-8"
  preview={(className) => (
    <div className={`grid gap-4 ${className}`}>
      <div className="bg-gray-300 h-12"></div>
      <div className="bg-gray-500 h-12"></div>
      <div className="bg-gray-300 h-12"></div>
      <div className="bg-gray-500 h-12"></div>
      <div className="bg-gray-300 h-12"></div>
      <div className="bg-gray-500 h-12"></div>
      <div className="bg-gray-300 h-12"></div>
      <div className="bg-gray-500 h-12"></div>
      <div className="bg-gray-300 h-12"></div>
      <div className="bg-gray-500 h-12"></div>
      <div className="bg-gray-300 h-12"></div>
      <div className="bg-gray-500 h-12"></div>
    </div>
  )}
/>

## カスタマイズ

デフォルトでは、Tailwindには、最大12の等しい幅の列を持つ基本的なグリッドを作成するためのgrid-template-columnユーティリティが含まれています。
Tailwindテーマ設定の`gridTemplateColumns`セクションをカスタマイズして、これらを変更、追加、または削除します。

ここから`grid-template-columns`CSSプロパティに直接アクセスできるため、カスタム列の値を一般的なものにすることも、複雑でサイト固有のものにすることもできます。

```diff
  // tailwind.config.js
  module.exports = {
    theme: {
      extend: {
        gridTemplateColumns: {
          // Simple 16 column grid
+         '16': 'repeat(16, minmax(0, 1fr))',

          // Complex site-specific column configuration
+         'footer': '200px minmax(900px, 1fr) 100px',
        }
      }
    }
  }
```

デフォルトテーマのカスタマイズについては[theme customization documentation](/docs/theme#customizing-the-default-theme)をご覧ください。

### レスポンシブと疑似バリアントクラス

<Variants plugin="gridTemplateColumns" name="grid-template-columns" />

### 無効にする

<Disabling plugin="gridTemplateColumns" name="grid-template-columns" />
